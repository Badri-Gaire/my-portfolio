---
import Container from "../../shared/Container.astro";

interface Props {
  className?: string;
  id?: string;
}

const { className = "", id } = Astro.props;
---

<section id={id} class={`py-5 ${className}`}>
  <Container>
    <slot />
  </Container>
</section>
